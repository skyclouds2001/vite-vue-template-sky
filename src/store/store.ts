import { createStore } from 'vuex'

export default createStore({
  state () {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {},
  plugins: [],
  strict: process.env.NODE_ENV !== 'production',
  devtools: true
})
